diff --git Features/GateOperations/Dtos/GateDtos.cs Features/GateOperations/Dtos/GateDtos.cs
index 3d4dece..004edbe 100644
--- Features/GateOperations/Dtos/GateDtos.cs
+++ Features/GateOperations/Dtos/GateDtos.cs
@@ -211,8 +211,7 @@ public sealed record LiveParkingDto(
     DateTime EntryTime,
     string DurationInside,
     DateTime? ValidUntil,
-    string Status,
-    decimal PendingAmount);
+    string Status);
 
 
 
@@ -231,7 +230,6 @@ public sealed record LiveParkingListItemDto(
     DateTime? ValidUntil,
     int OverstayDays,
     decimal OverstayAmount,
-    decimal PendingAmount,
     string PaymentStatus,
     string Status,
     string BarcodeStatus,
@@ -248,8 +246,7 @@ public sealed record PagedLiveParkingResultDto(
     int OverstayCount,
     int PaymentDueCount,
     int GeneratedCount,
-    int ExitedCount,
-    decimal TotalPendingAmount);
+    int ExitedCount);
 
 public sealed record PaymentListItemDto(
     int PaymentId,
@@ -306,7 +303,6 @@ public sealed record VehicleBarcodeListItemDto(
     string BarcodeStatus,
     int OverstayDays,
     decimal OverstayAmount,
-    decimal PendingAmount,
     string PaymentStatus,
     bool ForceExit,
     string? ForceExitReason,
@@ -325,8 +321,7 @@ public sealed record PagedVehicleBarcodeResultDto(
     int InsideCount,
     int ExitedCount,
     int InvalidCount,
-    int OverstayCount,
-    decimal TotalPendingAmount);
+    int OverstayCount);
 
 public sealed record VehicleBarcodeHistoryDto(
     DateTime Time,
diff --git Features/GateOperations/Services/GateOperationService.cs Features/GateOperations/Services/GateOperationService.cs
index 83f4e48..fbe6168 100644
--- Features/GateOperations/Services/GateOperationService.cs
+++ Features/GateOperations/Services/GateOperationService.cs
@@ -1485,22 +1485,15 @@ public sealed class GateOperationService(NetworldParkingDbContext db, IGateRepos
         }
 
         var sessions = await query.OrderByDescending(x => x.EntryTime).Take(500).ToListAsync(cancellationToken);
-        var result = new List<LiveParkingDto>();
-        foreach (var s in sessions)
-        {
-            var pending = await GetSubscriptionPendingAmountAsync(s.CompanyId, cancellationToken);
-            result.Add(new LiveParkingDto(
-                s.SessionId,
-                s.BarcodeNo,
-                s.PlateNo,
-                s.Company.CompanyName,
-                s.EntryTime ?? s.CreatedDate,
-                FormatDuration(DateTime.Now - (s.EntryTime ?? s.CreatedDate)),
-                s.Subscription?.EndDate,
-                s.Status,
-                pending));
-        }
-        return result;
+        return sessions.Select(s => new LiveParkingDto(
+            s.SessionId,
+            s.BarcodeNo,
+            s.PlateNo,
+            s.Company.CompanyName,
+            s.EntryTime ?? s.CreatedDate,
+            FormatDuration(DateTime.Now - (s.EntryTime ?? s.CreatedDate)),
+            s.Subscription?.EndDate,
+            s.Status)).ToList();
     }
 
 
@@ -1527,8 +1520,7 @@ public sealed class GateOperationService(NetworldParkingDbContext db, IGateRepos
             sorted.Count(x => x.OverstayDays > 0),
             CountLiveParkingCompaniesWithPending(sorted),
             sorted.Count(x => x.Status.Equals(ParkingConstants.SessionStatus.BarcodeGenerated, StringComparison.OrdinalIgnoreCase)),
-            sorted.Count(x => x.Status.Equals(ParkingConstants.SessionStatus.Exited, StringComparison.OrdinalIgnoreCase)),
-            SumLiveParkingPendingByCompany(sorted));
+            sorted.Count(x => x.Status.Equals(ParkingConstants.SessionStatus.Exited, StringComparison.OrdinalIgnoreCase)));
     }
 
     public async Task<IReadOnlyList<LiveParkingListItemDto>> ExportLiveParkingAsync(LiveParkingListQueryRequest request, CancellationToken cancellationToken = default)
@@ -1616,8 +1608,7 @@ public sealed class GateOperationService(NetworldParkingDbContext db, IGateRepos
             sorted.Count(x => x.Status.Equals(ParkingConstants.SessionStatus.Inside, StringComparison.OrdinalIgnoreCase)),
             sorted.Count(x => x.Status.Equals(ParkingConstants.SessionStatus.Exited, StringComparison.OrdinalIgnoreCase)),
             sorted.Count(x => x.BarcodeStatus.Equals(ParkingConstants.BarcodeStatus.Invalid, StringComparison.OrdinalIgnoreCase)),
-            sorted.Count(x => x.OverstayDays > 0),
-            SumVehicleBarcodePendingByCompany(sorted));
+            sorted.Count(x => x.OverstayDays > 0));
     }
 
     public async Task<IReadOnlyList<VehicleBarcodeListItemDto>> ExportVehicleBarcodesAsync(VehicleBarcodeListQueryRequest request, CancellationToken cancellationToken = default)
@@ -2430,7 +2421,7 @@ public sealed class GateOperationService(NetworldParkingDbContext db, IGateRepos
             rows = rows.Where(x => x.PaymentStatus.Equals(paymentStatus, StringComparison.OrdinalIgnoreCase)).ToList();
 
         if (tab.Equals("PaymentDue", StringComparison.OrdinalIgnoreCase))
-            rows = rows.Where(x => x.PendingAmount > 0).ToList();
+            rows = rows.Where(x => !x.PaymentStatus.Equals(ParkingConstants.PaymentStatus.Paid, StringComparison.OrdinalIgnoreCase)).ToList();
 
         return rows;
     }
@@ -2661,28 +2652,12 @@ public sealed class GateOperationService(NetworldParkingDbContext db, IGateRepos
     private static int CountLiveParkingCompaniesWithPending(IEnumerable<LiveParkingListItemDto> rows)
     {
         return rows
-            .Where(x => x.PendingAmount > 0)
+            .Where(x => !x.PaymentStatus.Equals(ParkingConstants.PaymentStatus.Paid, StringComparison.OrdinalIgnoreCase))
             .Select(x => x.CompanyId)
             .Distinct()
             .Count();
     }
 
-    private static decimal SumLiveParkingPendingByCompany(IEnumerable<LiveParkingListItemDto> rows)
-    {
-        return rows
-            .Where(x => x.PendingAmount > 0)
-            .GroupBy(x => x.CompanyId)
-            .Sum(x => x.Max(y => y.PendingAmount));
-    }
-
-    private static decimal SumVehicleBarcodePendingByCompany(IEnumerable<VehicleBarcodeListItemDto> rows)
-    {
-        return rows
-            .Where(x => x.PendingAmount > 0)
-            .GroupBy(x => x.CompanyId)
-            .Sum(x => x.Max(y => y.PendingAmount));
-    }
-
     private static LiveParkingListItemDto ToLiveParkingListItem(ParkingSession s, decimal pendingAmount)
     {
         var now = DateTime.Now;
@@ -2709,7 +2684,6 @@ public sealed class GateOperationService(NetworldParkingDbContext db, IGateRepos
             validUntil,
             overstayDays,
             overstayAmount,
-            pendingAmount,
             paymentStatus,
             s.Status,
             s.BarcodeStatus,
@@ -2747,7 +2721,6 @@ public sealed class GateOperationService(NetworldParkingDbContext db, IGateRepos
             s.BarcodeStatus,
             overstayDays,
             s.OverstayAmount,
-            pendingAmount,
             paymentStatus,
             s.ForceExit,
             s.ForceExitReason,
@@ -2766,7 +2739,6 @@ public sealed class GateOperationService(NetworldParkingDbContext db, IGateRepos
             "BarcodeNo" => desc ? rows.OrderByDescending(x => x.BarcodeNo) : rows.OrderBy(x => x.BarcodeNo),
             "CompanyName" => desc ? rows.OrderByDescending(x => x.CompanyName) : rows.OrderBy(x => x.CompanyName),
             "PlateNo" => desc ? rows.OrderByDescending(x => x.PlateNo) : rows.OrderBy(x => x.PlateNo),
-            "PendingAmount" => desc ? rows.OrderByDescending(x => x.PendingAmount) : rows.OrderBy(x => x.PendingAmount),
             "OverstayDays" => desc ? rows.OrderByDescending(x => x.OverstayDays) : rows.OrderBy(x => x.OverstayDays),
             _ => desc ? rows.OrderByDescending(x => x.EntryTime).ThenByDescending(x => x.SessionId) : rows.OrderBy(x => x.EntryTime).ThenBy(x => x.SessionId)
         };
@@ -2798,7 +2770,6 @@ public sealed class GateOperationService(NetworldParkingDbContext db, IGateRepos
             "PlateNo" => desc ? rows.OrderByDescending(x => x.PlateNo) : rows.OrderBy(x => x.PlateNo),
             "EntryTime" => desc ? rows.OrderByDescending(x => x.EntryTime) : rows.OrderBy(x => x.EntryTime),
             "ExitTime" => desc ? rows.OrderByDescending(x => x.ExitTime) : rows.OrderBy(x => x.ExitTime),
-            "PendingAmount" => desc ? rows.OrderByDescending(x => x.PendingAmount) : rows.OrderBy(x => x.PendingAmount),
             "Status" => desc ? rows.OrderByDescending(x => x.Status) : rows.OrderBy(x => x.Status),
             _ => desc ? rows.OrderByDescending(x => x.CreatedDate).ThenByDescending(x => x.SessionId) : rows.OrderBy(x => x.CreatedDate).ThenBy(x => x.SessionId)
         };
